/**
 * compare-board tests require the gstack `design` package which is not part of
 * gstack-browse. All tests in this file are skipped.
 */

import { describe, test } from 'bun:test';

describe.skip('compare-board (requires gstack design package)', () => {
  test('skipped', () => {});
});
